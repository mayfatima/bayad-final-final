import 'package:flutter/material.dart';

class BDOLenderPage extends StatelessWidget {
  const BDOLenderPage({super.key});


  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Detail for BDO')));
  }
}
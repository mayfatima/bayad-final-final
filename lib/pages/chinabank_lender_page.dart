import 'package:flutter/material.dart';

class ChinaBankLenderPage extends StatelessWidget {
  const ChinaBankLenderPage({super.key});


  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Detail for ChinaBank')));
  }
}
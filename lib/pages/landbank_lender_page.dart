import 'package:flutter/material.dart';

class LandBankLenderPage extends StatelessWidget {
  const LandBankLenderPage({super.key});


  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Detail for Landbank')));
  }
}
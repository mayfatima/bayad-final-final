import 'package:flutter/material.dart';

class PSBankLenderPage extends StatelessWidget {
  const PSBankLenderPage({super.key});


  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Detail for PSBank')));
  }
}
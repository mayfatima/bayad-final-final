import 'package:flutter/material.dart';

class RCBCLenderPage extends StatelessWidget {
  const RCBCLenderPage({super.key});


  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Detail for RCBC')));
  }
}
import 'package:flutter/material.dart';

class SecurityBankLenderPage extends StatelessWidget {
  const SecurityBankLenderPage({super.key});


  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Detail for Security Bank')));
  }
}
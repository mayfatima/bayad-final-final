import 'package:flutter/material.dart';

class UnionBankLenderPage extends StatelessWidget {
  const UnionBankLenderPage({super.key});


  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Detail for UnionBank')));
  }
}